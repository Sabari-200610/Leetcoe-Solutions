class Solution {
    public int[] minCosts(int[] cost) {
        int n=cost.length;
        int ans[]=new int[n];
        int minNum=Integer.MAX_VALUE;
        for(int i=0;i<n;i++){
            minNum=Math.min(minNum,cost[i]);
            ans[i]=minNum;
        }
        return ans;
    }
}