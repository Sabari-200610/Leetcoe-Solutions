class Solution {
    public int missingMultiple(int[] nums, int k) {
        Arrays.sort(nums);
        int mul=k;
        for(int num:nums){
            if(num==mul){
                mul+=k;
            }
        }
        return mul;
    }
}